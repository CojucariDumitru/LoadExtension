{
  "name": "load-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs && chokidar 'content/**' 'background/**' 'shared/**' -c 'node scripts/build.mjs'"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
